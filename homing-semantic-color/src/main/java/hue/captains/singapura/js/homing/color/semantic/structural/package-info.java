/**
 * The <strong>Structural</strong> group's members — colours that organize the UI
 * surface (hierarchy and legibility). These are the bulk of the shipped themes'
 * palette: the StudioVars surface / text / border roles. Near-neutral; they
 * carry no expressive meaning, only spatial hierarchy.
 */
package hue.captains.singapura.js.homing.color.semantic.structural;
