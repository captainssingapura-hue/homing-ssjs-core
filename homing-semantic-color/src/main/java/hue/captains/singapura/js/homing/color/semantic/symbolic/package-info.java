/**
 * The <strong>Symbolic</strong> group's members — colours whose meaning is
 * <em>learned / conventional</em>, not felt. The functional status set
 * ({@code danger / success / warning / info}) lives here, correctly classified:
 * it is a projection of the Affective plane onto a small learned vocabulary, and
 * its colour is a per-theme / per-culture authored mapping (e.g. ForbiddenCity
 * red = auspicious, not danger).
 */
package hue.captains.singapura.js.homing.color.semantic.symbolic;
